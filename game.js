// Assuming a simplified version of the function for demonstration purposes.

function nextRound() {
    // Reset landmark history
    landmarkHistory = [];

    // Ensure the wrist variable is correctly referenced
    if (typeof wristVisible !== 'undefined') {
        // Your logic here, using wristVisible instead of wristHigh
    }
}

// Other game.js code...